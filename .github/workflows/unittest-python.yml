name: Unit Tests:Python
on:
  workflow_call:

jobs:
  run:
    name: Run tests
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Unit Tests
        run: echo "Running full test suite"