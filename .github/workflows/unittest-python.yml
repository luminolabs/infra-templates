name: Unit Tests
on:
  workflow_call:

jobs:
  run:
    name: Unit Tests
    if: |
      (github.event.action == 'labeled' && github.event.label.name == 'draft') ||
      contains(github.event.pull_request.labels.*.name, 'draft')
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Unit Tests
        run: echo "Running full test suite"